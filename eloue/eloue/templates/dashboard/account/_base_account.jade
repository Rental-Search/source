extends ../jade/_base_dashboard

block content-view
  section.tabs-vertical-active.sm.scrollbar-custom: ul
    each tab in account_tabs
      li: a.tab-vertical(href="#{tab.href}.html")
        h3 #{tab.title}
  .content-view
    block sections