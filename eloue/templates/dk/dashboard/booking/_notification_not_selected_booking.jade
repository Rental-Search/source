section.pull-nothing
  .notification-empty
    .notification-container
      {% verbatim %}
      i(class="{{routeChangeInProgress ? 'loading' : ''}}").icon-5x.indent-sm-b.flaticon.stroke.calendar-5
      {% endverbatim %}
      h2(ng-show="!routeChangeInProgress") Aucune réservation sélectionnée