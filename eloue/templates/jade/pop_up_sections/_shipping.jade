h2 Livraison
.indent-b-md
    p.text-black Sélectionner un point relais ci-dessous :
    .zebra.indent-xs-t-
        {% verbatim %}
        label(ng-repeat="point in borrowerShippingPoints").zebra-row
            input(type="radio", name="pickup", ng-model="point.selected")
            .zebra-row-wrapper
                .zebra-col
                    span.radio-indicator
                .zebra-col
                    span.sprite-pickup-logo
                .zebra-col.main-cell: p
                    | {{point.name}}
                    br
                    | {{point.address}}
                    br
                    | {{point.zipcode}} {{point.city}}
                .zebra-col: p.indent-r-md +2,50&euro;
        {% endverbatim %}