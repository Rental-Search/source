-load i18n
form(name="sendMessageForm", ng-submit="sendMessage()", novalidate)
    .table-cell.main-cell
        textarea(name="", rows="4", placeholder="{% trans 'Rédigez votre message ici...' %}", ng-model="newMessage.body", ng-disabled="submitInProgress", required)
    .table-cell
        button(ng-disabled="sendMessageForm.$invalid || !sendMessageForm.$dirty", ng-disabled="submitInProgress || !newMessage.body", class="{% verbatim %}{{submitInProgress ? 'loading' : ''}}{% endverbatim %}").btn.bg-success {% trans "Envoyez" %}