.section-header
    h4 {% if is_pro %}Demandez un devis{% else %}Réservez en ligne{% endif %}
form.form-inline.section-body.text-center
    {% verbatim %}
    .part
        p
            small à partir de
            big.ng-cloak.text-primary &nbsp;{{pricePerDay}}&nbsp;/jour
        {% endverbatim %}
        include ../../_date_picker_border
        {% verbatim %}
        // p.available.unavailable Cet article est disponible à ces dates
        span(ng-show="dateRangeError").ng-cloak.text-danger {{dateRangeError}}
        {% endverbatim %}
        {% if not is_pro %}
        p(ng-show="available").ng-cloak.available Cet article est disponible à ces dates
        p(ng-show="!available").ng-cloak.available.unavailable Cet article n'est pas disponible à ces dates
        {% endif %}
        {% verbatim %}
    .part
        p
            | Durée de la location&nbsp;:&nbsp;
            b.period-days.ng-cloak {{duration}}
        p.price
            | Prix {% endverbatim %}{% if is_pro %}indicatif{% else %}de la location{% endif %}&nbsp;:{% verbatim %}
            big(ng-show="!!pricePerDay").text-primary.ng-cloak &nbsp;{{bookingPrice + shippingPrice | number}}&nbsp;€
        {% endverbatim %}
        if not is_pro
            p Assurance incluse
    .part
        a(href = "", data-dismiss="modal", ng-click="openModal('booking')").btn.bg-success.calendar-ok
            i.flaticon.stroke.calendar-2
            | {% if is_pro %}Envoyez{% else %}Réservez{% endif %}
        p.mrg-v-1
            if is_pro
                | Vous allez envoyer
                br
                | une demande de location. 
                br
                | &ndash;
                br
                | Le loueur vous recontactera 
                br
                | très rapidement 
                br
                | pour vous confirmer la location.
                br
                | &ndash;
                br
                | Le paiement se fera directement 
                br
                | chez le loueur.
        //-
            small Cette somme vous sera débitée lorsque le propriétaire validera la location
