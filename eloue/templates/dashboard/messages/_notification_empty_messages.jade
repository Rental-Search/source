section.pull-nothing(ng-hide="messageThreadList && messageThreadList.length > 0")
  .notification-empty
    .notification-container
      {% verbatim %}
      i(class="{{routeChangeInProgress ? 'loading' : ''}}").icon-5x.indent-sm-b.flaticon.stroke.mail
      {% endverbatim %}
      h2(ng-show="!routeChangeInProgress") Pas encore de conversation
      p(ng-show="!routeChangeInProgress").indent-md-v.text-black
        | Avant de faire une demande réservation, il est possible d'envoyer un message au propriétaire
      a(ng-show="!routeChangeInProgress", href="#").btn-inline.bg-success-hover.text-white Commencer à rechercher une annonce