block sections
  aside.address-detail.pull-nothing.scrollbar-custom.scrollbar-left
    .section-content
      form#reset-password-form.indent-md-b(data-ng-submit="resetPassword()", name="resetPasswordForm", ng-init="password_reset_form_fields=[{name: 'current_password', placeholder: 'Ancien mot de passe'}, {name: 'password', placeholder: 'Nouveau mot de passe'}, {name: 'confirm_password', placeholder: 'Nouveau mot de passe'}]")
       fieldset(data-ng-disabled="submitInProgress")
        h2.indent-md-b Modifaication de votre mot de passe
        .row(ng-repeat="field in password_reset_form_fields")
          .col-md-9.col-md-offset-3
            {% verbatim %}
            input.form-control(type="password", name="{{field.name}}", placeholder="{{field.placeholder}} *", required, autofocus)
            .text-danger(ng-show="!!errors.{{field.name}}", ng-bind="errors.{{field.name}}")
            {% endverbatim %}
        .row
          .col-xs-12.text-right
            button(type="reset").text.text-danger.text-underline-hover.indent-sm-h Annuler
            button(type="submit").btn-inline.bg-success-hover.text-white Enregistrer
