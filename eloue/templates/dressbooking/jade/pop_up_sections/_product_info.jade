include ../../jade/_mixins

{% verbatim %}
.thumbnail-img
    a(href="#"): img(style="background-image: url({{ product.picture }})").bg-image
.thumbnail-content
    h3 {{product.summary}}
    p.location
        i.flaticon.stroke.location-pin-3
        | {{product.address.city}}, {{product.address.zipcode}}
    p.stats
        {% endverbatim %}
        +average-rate("product.stats.average_rating")
        +rate-counter("product.stats.ratings_count")
        +number-of-comments("product.stats.booking_comments_count")
        {% verbatim %}
    .declarer
        .thumbnail-img
            a(href="#"): img(style="background-image: url({{product.owner.avatar.thumbnail}})").bg-image
        .thumbnail-content
            p {{product.owner.first_name}} {{product.owner.last_name}}
            .eloue-rents
                p.question Propriétaire e-loue
                .tooltip Ce propriétaire est membre d’e-loue. Dressbooking et e-loue partagent leur expertise dans le domaine de la location et mettent en commun leurs utilisateurs pour proposer une offre plus riche et un service de meilleur qualité.
{% endverbatim %}
